//테스트 예시
/**
describe('두 숫자 사칙연산 실행 함수', () => {
  it('덧셈 연산을 수행해야 한다', () => {
    expect(calculate(10, 4, '+')).toBe(14);
  });

  it('뺄셈 연산을 수행해야 한다', () => {
    expect(calculate(10, 4, '-')).toBe(6);
  });

  it('곱셈 연산을 수행해야 한다', () => {
    expect(calculate(10, 4, '*')).toBe(40);
  });

  it('나눗셈 연산을 수행해야 한다', () => {
    expect(calculate(10, 4, '/')).toBe(2.5);
  });

  it('잘못된 연산자에 대해 에러를 반환해야 한다', () => {
    expect(() => calculate(10, 4, 'ㅇ')).toThrow(Error('잘못된 연산자입니다'));
  });

  it('나누기 0에 대한 에러 반환', () => {
    expect(() => calculate(10, 0, '/')).toThrow(Error('0으로 나눌 수 없습니다'));
  });

  it('유효하지 않은 숫자 입력에 대한 에러 반환', () => {
    expect(() => calculate('abc', 4, '+')).toThrow(Error('유효한 숫자를 입력하세요'));
  });
});*/
